# SHARED.md — The Product Lab Relaunch

> Colin reads this every morning brief. Keep it current. Max 150 lines.

## What's Waiting on Dan
- **Licensing review (NEW, HIGH)** — 178 catalog products use third-party IP (Nike, Star Wars, Disney/Marvel, football clubs, Coca-Cola, K-pop, etc.). Held in draft, excluded from launch. Decision before Drop 2: pull / license / replace with original designs.
- **Production hosting decisions** — Supabase project + managed Redis + deploy target (Vercel/Railway) for the Medusa launch (D-025).
- **Razorpay + Shiprocket credentials** — for payment/shipping integration testing.
- **Product photography** — Dark posterboard + desk lamp setup. Asset list has exact shot list for 7 hero SKUs.
- *(Resolved — Product copy)* All 67 Drop 1 PDP descriptions written by Joanna + live in Medusa.
- *(Resolved — Notification provider)* SMTP/Resend/SendGrid module built and wired for transactional emails. Plus **WhatsApp notification module (D-033)** built for event-driven order confirmations, tracking, delivery, and marketing messages via Gupshup/MSG91. Both go live by pasting credentials.

## Status
**Phase 4 Active (Build) — Medusa platform (D-025).** Catalog **curated for Drop 1 (D-028, 2026-06-13):** Medusa now has **67 published products across 5 live collections** (Certified Disaster, Soft Serve, Big Mood, Main Character Energy, Cat & Dog People) + **435 draft backlog**. This **meets the Phase 4 gate criterion "≥3 collections live with correct products"** — James still owns the overall gate (Razorpay/Shiprocket/COD/Airtable/GA4/LCP/QA remain open). Storefront connects via publishable key + India/INR region. **All 67 now carry brand-voice PDP descriptions (2026-06-13).**

## What's Done
- **WhatsApp notification system built (D-033, 2026-06-20)** — Event-driven WhatsApp messaging across the entire order lifecycle via new `whatsapp-notification` module (Gupshup/MSG91 with stub mode). **21 WhatsApp templates** covering 10 touchpoints: order confirmation (prepaid/COD), tracking, delivery, refund, return, exchange, damage/wrong-item resolution, cart abandon, COD reminder, drop announcements, re-engagement, upsell, support escalation. **3 new subscribers** fire on `order.placed`, `fulfillment.created`, and `order.updated`→"completed". WhatsApp consent checkbox added to checkout flow (stored on cart → order metadata). Profile consent toggle guards marketing sends; transactional messages always deliver. Bedrock: `backend/medusa/src/modules/whatsapp-notification/` + `backend/medusa/src/subscribers/order-confirmation.ts`, `fulfillment-notification.ts`, `order-delivery.ts`.
- **Direct Meta WhatsApp transport added (D-034, 2026-06-20)** — New `meta-transport.ts` sends via `graph.facebook.com/v22.0` with zero BSP markup. Auto-maps template names (dots→underscores). Retry logic for 429/5xx. Setup SOP at `docs/ops-sop/whatsapp-business-api-setup.md`. All 3 subscribers + config updated to recognize Meta credentials. Provider selectable via `WHATSAPP_PROVIDER=meta`.
- **Notification provider wired (D-032, 2026-06-20)** — SMTP/Resend/SendGrid module built. Password-reset and order-transfer emails now deliver when provider configured. Three transport methods: SMTP (Nodemailer), Resend API, SendGrid API. **Stub mode** when no credentials set — tokens still generated, URLs logged to stdout + `notification-audit/*.jsonl` for dev. No code change to go live — paste credentials, restart. Custom backend routes for token lifecycle: `/store/reset-password`, `/store/verify-reset-token`, `/store/update-password`, `/store/order-transfer-request`, `/store/accept-order-transfer`. Storefront updates: forgot-password uses custom route, reset-password verifies token on mount, new `/orders/accept-transfer` page for email link landing. Bedrock: `backend/medusa/src/modules/smtp-notification/`.
- **Customer login + account management — built & verified on Medusa (D-031, 2026-06-20)** — closes the biggest D-029 P0 gap (consistent auth/session). The customer's Medusa JWT now persists in an httpOnly cookie and every authenticated call carries it per-request (`storefront/src/lib/medusa-customer.ts`) — the old shared-singleton-in-memory approach was a cross-account leak risk *and* the reason orders/profile/addresses never worked. Built: login/register/logout/me, forgot+reset password, **change-password (fixes "User not found")**, profile edit + WhatsApp/marketing consent, address book, real order history + detail (timeline/tracking/WhatsApp share/reorder), guest `/track`, **claim guest order by email**, checkout→customer attachment + saved-address prefill, DPDP export + account-deletion. Verified live with Playwright (login→claim order #1→history→detail→add address→change password). No in-memory auth store remains anywhere (with D-030). Did **not** touch COD checkout (D-029) or catalog (D-028). Audit: `artifacts/phase-4/customer-auth-account-audit.md`.
- **Admin auth + operator ops cockpit on Medusa — built & verified (D-030, 2026-06-20)** — legacy in-memory `auth-store` (fake `admin123`/`manager123`) **deleted**; admin login runs on Medusa user auth with role from the Medusa user's `metadata.role` (sole operator → superadmin). New server-side `lib/medusa-admin.ts` service-account client backs all `/api/admin/*`. The custom `/admin` now owns the **COD-first daily ops loop on REAL Medusa orders**: orders list/detail, **mark COD collected** (captures the payment), add note (timeline), mark fulfilled, ship+AWB (free-text until Shiprocket), cancel, complete, plus a real dashboard (today / COD-pending / revenue). Catalog, customer edits, refunds & team/user management **defer to Medusa admin `:9000/app`** (read-only mirrors in `/admin`; writes 405). Admin password change works via a Medusa backend route (`authService.updateProvider`). **Fixed a real security bug:** a stray root `middleware.ts` only checked *token presence*, letting any logged-in customer reach `/admin` — replaced with one role-aware `src/proxy.ts`. **Verified live** on order #1: COD captured (₹500, DB-confirmed), note + fulfillment persisted (visible at `:9000/app`), RBAC denies customer (403) and manager-on-superadmin-routes (403). Admin temp password: `admin@theproductlab.in` / `TplAdmin#Temp2026`. Audit: `artifacts/phase-4/admin-auth-ops-audit.md`. Did **not** touch COD checkout (D-029) or catalog (D-028).
- **Checkout line works end-to-end for COD (D-029, 2026-06-19)** — placed a real order through the live storefront (browse→PDP→cart→checkout→COD→order-confirmed; order_01KVGATWG2CVKDKFBE4P2W25K0). Fixed a Medusa shipping-options resolution bug (rule-free options), linked COD to the India region, fixed the COD-floor validator (computed total) + the checkout's invalid `customer` field + missing publishable-key headers on custom `/store/*` calls. COD ₹299 floor enforced. **Razorpay is a ready stub — only key/secret/webhook to wire.** Gap: free-shipping ≥₹499 not yet enforced in Medusa (flat ₹50).
- **Drop 1 PDP copy — all 67 live (2026-06-13)** — Joanna wrote brand-voice 4-part descriptions (Identity Hook / Why This / WhatsApp Line / Specs) per the locked `verbal-identity` system; Andy grounded specs in production data (material correction: UV-printed acrylic + premium vinyl, not enamel). Loaded via idempotent `backend/medusa/src/scripts/apply-pdp-copy.ts`; verified 67/67 published products have a description. Artifact `artifacts/phase-4/drop-1-pdp-copy.md` (approved) + load file `drop1-descriptions.json`. Residual: per-SKU print dimensions to backfill (low, stripped from live text).
- **Drop 1 catalog curation (D-028)** — 502 raw products → 67 published text-opinion designs in 5 collections, 435 drafted (178 licensed-IP held for licensing review, 104 object, 153 dup variants), 4 demo apparel deleted. Cleaned titles. Reproducible: `backend/medusa/src/scripts/curate-drop1.py` + `apply-drop1.ts`; reversible snapshot in `artifacts/phase-4/catalog-curation/`. Doc: `artifacts/phase-4/drop-1-merchandising-curation.md`.
- **Real catalog imported into Medusa** — 502 products via a repeatable pipeline (`backend/medusa/src/scripts/prep-woo-catalog.py` + `import-woo.ts`). What's-pulled-vs-not is documented in `knowledge/26-CURRENT-STATE.md` → Catalog Status. Caveat: product images hotlinked from old WordPress, port to CDN before launch.
- **Storefront SSR fix** — `jwtTokenStorageMethod` set to `memory` so server components can call Medusa.
- **Storefront MVP wired** — real TPL products with prices, PDP copy, themes. Collections/sets/gift pages filter by actual product relationships. Medusa fallback renders local data when backend is down.
- **TPL shared drive fully indexed** — 24 folders, ~200+ files mapped in `TPL-SHARED-DRIVE-INDEX.md` + changelog in `TPL-SHARED-DRIVE-LOG.md`. Key discovery: `dan 25/` has 15 strategic Google Docs (SOPs, KB, business manual, org structure, warehouse ops).
- **Handoffs written** — `handoffs/2026-06-06-wired-mvp-handoff.md` (architecture, data flow, product mappings) and `handoffs/2026-06-06-build-list-updated.md` (26-item P0/P1/P2/P3 build list).
- Earlier: 16 storefront pages built, tooling research sprint (3 agents), command center dashboard, competitor threat deep dives, 3 Dan unblocker tools, Fynd/Medusa/open-source research.

## Recent Decisions
- D-034: Direct Meta WhatsApp Cloud API transport added — new `meta-transport.ts`, zero BSP markup, auto-name-mapping, retry logic, setup SOP
- D-033: Event-driven WhatsApp notification system built — Gupshup/MSG91 module, 21 templates, 3 subscribers, checkout consent, order lifecycle automation
- D-032: Notification provider wired — SMTP/Resend/SendGrid module; password-reset + order-transfer emails now deliver when provider configured; stub mode for dev
- D-031: Customer auth + account management consolidated on Medusa (built + verified live)
- D-030: Admin auth consolidated on Medusa; legacy in-memory auth-store deleted
- D-029: COD checkout/fulfillment verified end-to-end (real order placed)
- D-028: Drop 1 catalog curation rules — licensed IP → draft (excluded, flag licensing review); Drop 1 = text-opinion across all accessory types; 5 collections live, 67 products
- D-027: AEO / Merchant Center / crawler / agent-access = Phase 4 launch requirements
- D-026: Mission Control dashboard live at `/mission-control` — 6-tab project command center
- D-025: Platform reversal — Medusa + Supabase launch; Fynd dropped (supersedes D-022)
- D-024 (proposed): Drop 1 lead with Collect Sets + Gift Persona Shopping + Opinion Wall
- D-023: Competitor landscape completed

## Blockers
- **Dan: Production infra** — Supabase + Redis + deploy target needed to take Medusa live (catalog runs locally now).
- **Dan: Razorpay + Shiprocket creds** — for payment/shipping testing.
- **Dan: Photography** — 7 hero SKUs need shooting (asset-list.md has exact brief)
- **Dan: Copy** — Fast-start template created in Phase 4 artifacts
- **Dan: Email/notification provider (NEW, MEDIUM)** — password-reset delivery + Medusa's token-based order transfer are built but dormant until an email provider is wired. Customer change-password, guest `/track`, and claim-by-email all work *without* it.
- *(Resolved: "Fynd credentials" blocker is dead — Fynd dropped per D-025.)*

## Cross-Project Dependencies
- None active. TPL is independent from YDS per D-011.
